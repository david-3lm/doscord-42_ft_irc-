#ifndef Channel_HPP
#define Channel_HPP

class Channel 
{
public:
    Channel();
    ~Channel();
    Channel(const Channel &other);
    Channel &operator=(const Channel &other);
};

#endif // Channel_HPP
